<%= form_for(@contestant) do |f| %>
  <% if @contestant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contestant.errors.count, "error") %> prohibited this contestant from being saved:</h2>

      <ul>
      <% @contestant.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :has_downloaded %><br>
    <%= f.check_box :has_downloaded %>
  </div>
  <div class="field">
    <%= f.label :has_shared %><br>
    <%= f.check_box :has_shared %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
